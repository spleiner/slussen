{
  // Put uv's cache on the same filesystem as your repo/venv
  "containerEnv": {
    "UV_CACHE_DIR": "/workspaces/.uv_cache"
  },
  "postCreateCommand": "mkdir -p /workspaces/.uv_cache && curl -LsSf https://astral.sh/uv/install.sh | sh && uv python install 3.13 && uv sync"
}
